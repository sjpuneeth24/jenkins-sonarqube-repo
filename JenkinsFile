pipeline {
    agent any 
    stages {
        stage('Clean') { 
            steps {
                withGradle {
                    sh './gradlew build'
                }
         	}
        }
    }
}
